<div class="person-close">
    <button class="button"
        id="person-close-button"
        type="button"
        data-href="{{ route('index') }}"
        data-href-part="{{ route('partials.person.close') }}"
        title="{{ __('person.crud.close') }}">
        <img class="icon-sm" src="{{ asset('img/person/close.svg') }}" alt="close">
    </button>
</div>